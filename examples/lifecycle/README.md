# Lifecycle examples

`node.json` and `python.json` deliberately carry the same lifecycle, package, acceptance, and evidence shape. The stack names affect commands and implementation details only; the public lifecycle contract does not assume a language or framework.
